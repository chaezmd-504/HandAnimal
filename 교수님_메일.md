# 교수님 메일

현재 검토 중인 구현 방식들을 정리하여 말씀드립니다.

대체로 수치를 계산하고 직접 관절에 적용하는 방식이나, 방식에 따라서 회전각 그 자체를 매핑할지, 아니면 가장 근접한 애니메이션 키프레임을 활용해서 보정하는 방법도 있습니다. 모든 방식에서 애니메이션은 재생되지 않기 때문에 손 움직임에 대한 학습 데이터도 필요하지 않습니다. 그 순간의 손 포즈와 키프레임 포즈 라이브러리의 가장 근사한 키프레임과의 비교이기 때문입니다.

---

## 두 가지 방향성

- **손 우선**: 손과 동물이 항상 직결된 느낌(Embodiment) 극대화. 손 떨림이 전달되는 대신 즉각 반응.
- **애니메이션 우선**: 아티스트가 만든 포즈 활용. 손 떨림 억제.

---

## 1. Direct (구현 완료) — 손 우선

`동물 관절 각도 = 기준 각도 + (현재 손 각도 − 기준 손 각도) × scale`

손의 변화량이 수식 하나로 동물 관절에 즉시 전달됩니다.

- 장점: 구현이 단순하고 손에 즉각 반응합니다. 손과 동물이 항상 연결된 느낌이 가장 강합니다.
- 단점: 손 떨림이 그대로 전달되며, 각 관절이 독립적으로 반응하여 자연스러운 연동 동작 표현에 한계가 있습니다.

---

## 2. Keyframe 블렌딩 (구현 완료) — 애니메이션 우선

Walk, Attack, Idle 등 모든 애니메이션의 키프레임 포즈를 하나의 통합 라이브러리로 보유합니다. 매 프레임 현재 손 포즈와 각 포즈 사이의 거리를 계산하고, 가까울수록 높은 가중치를 부여해 블렌딩합니다.

- 장점: 결과가 항상 디자이너가 만든 포즈들의 조합이므로 이상한 포즈가 나올 확률이 낮습니다. 손 떨림이 억제되며, Walk/Attack 포즈가 같은 라이브러리에 있어 손 모양에 따라 자연스럽게 전환됩니다.
- 단점: 키프레임 간 순서 개념이 없어 Walk 포즈들이 W₁→W₂→W₃ 순서로 나온다는 보장이 없습니다. 즉 애니메이션처럼 자연스럽게 걷는 것같이 보이지 않을 수 있습니다.

---

## 1+2. Direct + Keyframe 혼합

`출력 = (1−α) × Direct결과 + α × 가장 가까운 키프레임 포즈`

1과 2 사이의 연속선상에 있는 방식입니다. α=0이면 순수 Direct, α=1이면 순수 Keyframe이 되며, α를 조절하여 반응성과 자연스러움 사이의 균형을 맞출 수 있습니다.

---

## 3. Sequential strict — 애니메이션 우선

키프레임에 애니메이션 이름과 시간 순서를 부여합니다(W₁→W₂→W₃→W₄). 시스템이 현재 시퀀스 내 위치를 기억하며, 매 프레임 손 포즈의 이동 방향에 따라 이 위치를 조금씩 증감시킵니다. 항상 인접한 두 키프레임 사이에서만 블렌딩이 발생하므로, W₁에서 W₄로 갑자기 점프하는 현상이 구조적으로 불가능합니다. 손가락을 구부릴수록 W₁→W₄ 방향으로 진행되고, 손이 멈추면 해당 위치에서 정지합니다.

- 장점: Walk 시퀀스의 시간 순서가 보장되어 실제로 걷는 동작처럼 보입니다. 포즈 간 튀는 현상이 없습니다.
- 단점: 한 시퀀스 내에서만 이동 가능하여 Walk 도중 Attack으로 전환하기 어렵습니다.

---

아직 유니티 회전축 문제가 해결되지 않아서 이 문제 해결 후 테스트해보면서 어떤 것이 괜찮은지 생각해볼 예정입니다. 논문 영상을 다시 보니 저의 Direct 방식에 흔들림 보정을 한 것 같은데, 제 캡스톤 같은 경우에는 움직임까지 구현하는 것이 목표이기 때문에 자연스러운 움직임을 표현하려면 손가락이 움직이는 그대로 움직이는 것보다 애니메이션을 조금 따라가는 게 움직임 구현할 때 편하지 않을까라고 생각을 해서 다양한 방법에 대해 고민을 해보았습니다.
